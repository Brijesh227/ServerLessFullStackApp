{
  "name": "serverless-food-order-system",
  "version": "1.0.0",
  "description": "",
  "main": "dist/local.js",
  "scripts": {
    "build": "npm run cleanup && tsc",
    "cleanup": "rm -rf dist && find . -name '*.js' -not -path './node_modules/*' -delete ",
    "dev": "concurrently \"npx tsc --watch\" \"nodemon -q ./dist/local.js\"",
    "start:dev": "npm run build && nodemon dist/local.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amazon-cognito-identity-js": "^6.3.1",
    "aws-sdk": "^2.1429.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.1",
    "jwk-to-pem": "^2.0.5",
    "mongoose": "^7.4.1",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.6",
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1",
    "typescript": "^5.1.6"
  }
}
